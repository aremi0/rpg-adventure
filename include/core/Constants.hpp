#pragma once

namespace Config {
    // Dimensioni e Grid standard
    static constexpr unsigned int kTileSize = 64;

    namespace MainMenu {
        static constexpr int kBackgroundFrames = 48;
        static constexpr float kFrameDuration = 0.1f; // 10 FPS
    }
}
